from pathlib import Path

from ..types import Script


# STUB: replace with Claude API call that takes ReferenceAnalysis and a product
# brief, then returns a similar-structured script (hook / body / cta).
def generate_script(analysis_path: Path) -> Script:
    raise NotImplementedError(
        "auto script generation not yet implemented - pass --from-user PATH for now"
    )
